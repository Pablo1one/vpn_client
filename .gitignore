# Flutter/Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
build/
*.g.dart
*.freezed.dart
pubspec.lock

# Android
android/local.properties
android/.gradle/
android/captures/
android/key.properties

# iOS
ios/Flutter/
ios/.symlinks/
ios/Pods/
ios/Runner/GeneratedPluginRegistrant.*

# Windows
windows/flutter/

# macOS
macos/Flutter/
.DS_Store

# IDE
.idea/
.vscode/
*.iml

# Sensitive / large binaries (download separately via scripts/)
.env
*.env
singbox/
# собранные инсталляторы живут в GitHub Releases, в репозитории не нужны
installer_output/
tool/icon_preview*.png
# старые движки 3-движковой схемы (до унификации) - не используются
assets/bin/sing-box.exe
assets/bin/xray.exe
# движки коммитим в репо: singbox-uni.exe (windows), wintun.dll, libbox.aar (android) - см. README
# (LibBox.xcframework для iOS пока тянется отдельно)
ios/LibBox.xcframework/

# регенерируемые исходники Android-иконки (gen_icon.dart)
tool/android_*.png

# android release signing (НЕ коммитить — бэкапить отдельно!)
android/key.properties
android/app/*.jks
